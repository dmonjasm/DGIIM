<nombre> será el nombre que el superusuario le haya asignado
<group> se creará un grupo primario de usuarios con el mismo nombre del usuario que se añadirá al sistema
<UID> el siguiente inmediatamente al ultimo usuario registrado
<GID> el mismo que el UID
<home directory> /home
<numbre of days after a password expire until the account disabled> -1
<expire> vacio
<SKEL> se copiarán en el directorio del nuevo usuario desde /etc/skel
<bash> /bin/bash
